# .gitignore

# --------------------------------------------------------------------
# Terraform - Core
# --------------------------------------------------------------------

# Local .terraform directories (Contains the large provider binaries)
**/.terraform/*

# .tfstate files (Contains sensitive data/secrets - NEVER commit these)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.log.*

# Exclude all .tfvars files, which are likely to contain sentitive data,
# such as password, private keys, and other secrets.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally 
# and so are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files
.terraformrc
terraform.rc

# --------------------------------------------------------------------
# IDE & OS System Files (Optional but recommended)
# --------------------------------------------------------------------

# macOS
.DS_Store

# Windows
Thumbs.db

# VS Code
.vscode/

# JetBrains / IntelliJ
.idea/
